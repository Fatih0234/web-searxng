{
  "id": "b89c0936",
  "title": "Phase 8 — Tests, docs, polish & final acceptance",
  "tags": [
    "phase-8",
    "tests",
    "docs"
  ],
  "status": "open",
  "created_at": "2026-08-20T09:24:06.681Z",
  "assigned_to_session": "01a01e7a-4fae-71bb-b9f1-28f32ccc0422"
}

**Goal:** Deterministic unit tests + marked integration + docs/polish + end-to-end acceptance from clean runtime.

**Unit tests (no Docker/internet, 07):**
- config, lifecycle (mocked), search normalization (fixtures), URL security matrix + redirect validation, reader (mocked HTTP, extraction, truncation), CLI stdout/stderr & exit codes, MCP (mocked core)
- must cover SSRF deny cases explicitly

**Integration tests (marked `integration`, skip unless enabled):**
- local SearXNG smoke: init → status stopped → search stable query validates JSON → running → second search reuse → stop → stopped
- live reader: read https://example.com clean text extraction

**Docs & polish (02 Phase8 + 08):**
- Translate instruction pack into concise README + docs/operation (not copy wholesale); include install via `uv tool install .` / `uv sync`; usage for minimal agent vs MCP; lifecycle service; troubleshooting via doctor; record verified SearXNG image tag/digest; document manual update path; never auto-pull each search
- Add prompts/ and skills/ stripped to user-facing versions if desired
- Verify assets compose/settings meet 04 constraints (127.0.0.1, no Valkey, limiter false, json enabled, image_proxy false, public_instance false)
- Final check: ensure search/read artifacts meet spec, logging quiet default, no secret leakage, image configurable via SEARXNG_IMAGE/WEBX_

**Manual acceptance (07 checklist):**
webx --help; webx init; webx doctor; webx status (stopped); webx search ... --pretty; webx status (running); webx read https://...; webx read denials (127.0.0.1:8888, 192.168.1.1, file:///etc/passwd must exit 5); webx stop; webx status (stopped); MCP Inspector 2-tool verification + lazy-start + ownership.

**Non-goals to defer (09):** browser, PDF, crawling, reranker, cache, lease, LLM summarizer.

Spec refs: 02 Phase8, 07 full, 08 operations, 09 decisions
