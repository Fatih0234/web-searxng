{
  "id": "6f9d48ba",
  "title": "Phase 1 — Project foundation: pyproject, src layout, errors, models, assets",
  "tags": [
    "phase-1",
    "foundation"
  ],
  "status": "done",
  "created_at": "2026-08-20T09:23:33.585Z"
}

**Goal:** Create `src/` package that is importable and `webx --help` works before Docker.

**Files to create:**
- `pyproject.toml` — `httpx>=0.28,<1`, `trafilatura>=2.2,<3`, `platformdirs>=4,<5`, optional `mcp>=2,<3`, `[project.scripts]` webx, webx-mcp, `pytest`, `pytest-cov`
- `src/webx/__init__.py`, `errors.py` (typed exceptions mapped to exit codes 2-7), `models.py` (SearchQuery/Result/Response, ReadRequest/Response, RuntimeStatus, DoctorReport), `config.py` (env WEBX_*, platformdirs, defaults)
- `src/webx/assets/compose.yml` + `settings.yml` (from 04 spec, importlib.resources)
- `README.md` skeleton, `.gitignore`
- `tests/unit/` placeholder

**Decisions:**
- dataclasses / typed dicts, no pydantic to keep deps narrow
- version sourced from pyproject
- assets via importlib.resources

**Validation:** `uv sync && uv run webx --help` succeeds; `pytest` empty run passes.

**Spec refs:** 02 Phase1, 03 Models/Exit codes, 04 compose/settings templates
